# Font Assets

This directory contains raw font files (.ttf) used by WGLYMR.

Supported fonts:

- `roboto.ttf` - Primary UI font
- `inter.ttf` - Alternative UI font
- `mono.ttf` - Monospace font for code/debug

These files are processed by the font generation pipeline (xtask fonts) to create MSDF and bitmap atlases.
